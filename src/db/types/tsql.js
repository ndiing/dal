
const TSQLTypes = {
    varChar: (length) => ({ type: "VARCHAR", length }),
    nVarChar: (length) => ({ type: "NVARCHAR", length }),
    text: () => ({ type: "TEXT" }),
    int: () => ({ type: "INT" }),
    bigInt: () => ({ type: "BIGINT" }),
    tinyInt: () => ({ type: "TINYINT" }),
    smallInt: () => ({ type: "SMALLINT" }),
    bit: () => ({ type: "BIT" }),
    float: () => ({ type: "FLOAT" }),
    numeric: (precision, scale) => ({ type: "NUMERIC", precision, scale }),
    decimal: (precision, scale) => ({ type: "DECIMAL", precision, scale }),
    real: () => ({ type: "REAL" }),
    date: () => ({ type: "DATE" }),
    dateTime: () => ({ type: "DATETIME" }),
    dateTime2: (scale) => ({ type: "DATETIME2", scale }),
    dateTimeOffset: (scale) => ({ type: "DATETIMEOFFSET", scale }),
    smallDateTime: () => ({ type: "SMALLDATETIME" }),
    time: (scale) => ({ type: "TIME", scale }),
    uniqueIdentifier: () => ({ type: "UNIQUEIDENTIFIER" }),
    smallMoney: () => ({ type: "SMALLMONEY" }),
    money: () => ({ type: "MONEY" }),
    binary: (length) => ({ type: "BINARY", length }),
    varBinary: (length) => ({ type: "VARBINARY", length }),
    image: () => ({ type: "IMAGE" }),
    xml: () => ({ type: "XML" }),
    char: (length) => ({ type: "CHAR", length }),
    nChar: (length) => ({ type: "NCHAR", length }),
    nText: () => ({ type: "NTEXT" }),
    tvp: (tvpType) => ({ type: "TVP", tvpType }),
    udt: () => ({ type: "UDT" }),
    geography: () => ({ type: "GEOGRAPHY" }),
    geometry: () => ({ type: "GEOMETRY" }),
    variant: () => ({ type: "VARIANT" }),
};
module.exports = TSQLTypes;
