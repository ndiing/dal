const PLpgSQLTypes = {
    bigInt: () => ({ type: "BIGINT" }),
    int8: () => ({ type: "INT8" }),
    bigserial: () => ({ type: "BIGSERIAL" }),
    serial8: () => ({ type: "SERIAL8" }),
    bit: (n) => ({ type: "BIT", n }),
    bitVarying: (n) => ({ type: "BIT VARYING", n }),
    varBit: (n) => ({ type: "VARBIT", n }),
    boolean: () => ({ type: "BOOLEAN" }),
    bool: () => ({ type: "BOOL" }),
    box: () => ({ type: "BOX" }),
    bytea: () => ({ type: "BYTEA" }),
    character: (n) => ({ type: "CHARACTER", n }),
    char: (n) => ({ type: "CHAR", n }),
    characterVarying: (n) => ({ type: "CHARACTER VARYING", n }),
    varChar: (n) => ({ type: "VARCHAR", n }),
    cidr: () => ({ type: "CIDR" }),
    circle: () => ({ type: "CIRCLE" }),
    date: () => ({ type: "DATE" }),
    doublePrecision: () => ({ type: "DOUBLE PRECISION" }),
    float: () => ({ type: "FLOAT" }),
    float8: () => ({ type: "FLOAT8" }),
    inet: () => ({ type: "INET" }),
    integer: () => ({ type: "INTEGER" }),
    int: () => ({ type: "INT" }),
    int4: () => ({ type: "INT4" }),
    interval: () => ({ type: "INTERVAL" }),
    intervalYear: (p) => ({ type: "INTERVAL", fields: "YEAR", p }),
    intervalMonth: (p) => ({ type: "INTERVAL", fields: "MONTH", p }),
    intervalDay: (p) => ({ type: "INTERVAL", fields: "DAY", p }),
    intervalHour: (p) => ({ type: "INTERVAL", fields: "HOUR", p }),
    intervalMinute: (p) => ({ type: "INTERVAL", fields: "MINUTE", p }),
    intervalSecond: (p) => ({ type: "INTERVAL", fields: "SECOND", p }),
    intervalYearToMonth: (p) => ({ type: "INTERVAL", fields: "YEAR TO MONTH", p }),
    intervalDayToHour: (p) => ({ type: "INTERVAL", fields: "DAY TO HOUR", p }),
    intervalDayToMinute: (p) => ({ type: "INTERVAL", fields: "DAY TO MINUTE", p }),
    intervalDayToSecond: (p) => ({ type: "INTERVAL", fields: "DAY TO SECOND", p }),
    intervalHourToMinute: (p) => ({ type: "INTERVAL", fields: "HOUR TO MINUTE", p }),
    intervalHourToSecond: (p) => ({ type: "INTERVAL", fields: "HOUR TO SECOND", p }),
    intervalMinuteToSecond: (p) => ({ type: "INTERVAL", fields: "MINUTE TO SECOND", p }),
    json: () => ({ type: "JSON" }),
    jsonb: () => ({ type: "JSONB" }),
    line: () => ({ type: "LINE" }),
    lseg: () => ({ type: "LSEG" }),
    macaddr: () => ({ type: "MACADDR" }),
    macaddr8: () => ({ type: "MACADDR8" }),
    money: () => ({ type: "MONEY" }),
    numeric: (p, s) => ({ type: "NUMERIC", p, s }),
    decimal: (p, s) => ({ type: "DECIMAL", p, s }),
    path: () => ({ type: "PATH" }),
    pgLsn: () => ({ type: "PG_LSN" }),
    pgSnapshot: () => ({ type: "PG_SNAPSHOT" }),
    point: () => ({ type: "POINT" }),
    polygon: () => ({ type: "POLYGON" }),
    real: () => ({ type: "REAL" }),
    float4: () => ({ type: "FLOAT4" }),
    smallInt: () => ({ type: "SMALLINT" }),
    int2: () => ({ type: "INT2" }),
    smallserial: () => ({ type: "SMALLSERIAL" }),
    serial2: () => ({ type: "SERIAL2" }),
    serial: () => ({ type: "SERIAL" }),
    serial4: () => ({ type: "SERIAL4" }),
    text: () => ({ type: "TEXT" }),
    time: (p) => ({ type: "TIME", p }),
    timeWithTimeZone: (p) => ({ type: "TIME WITH TIME ZONE", p }),
    timetz: () => ({ type: "TIMETZ" }),
    timestamp: (p) => ({ type: "TIMESTAMP", p }),
    timestampWithTimeZone: (p) => ({ type: "TIMESTAMP WITH TIME ZONE", p }),
    timestamptz: () => ({ type: "TIMESTAMPTZ" }),
    tsquery: () => ({ type: "TSQUERY" }),
    tsvector: () => ({ type: "TSVECTOR" }),
    txidSnapshot: () => ({ type: "TXID_SNAPSHOT" }),
    uuid: () => ({ type: "UUID" }),
    xml: () => ({ type: "XML" }),
};
module.exports = PLpgSQLTypes;
